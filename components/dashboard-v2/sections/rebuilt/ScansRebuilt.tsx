// DASHFIN stub — will be replaced by the BB v4 elevate rebuild of this section.
// Until the builder lands, re-hosts the v1 panel so the shell keeps working.
export { default } from '../../../dashboard/AudienceAuditsPanel';
